{
  "name": "strategyforge",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "bun test",
    "test:compute": "cd packages/compute && bun test"
  }
}
